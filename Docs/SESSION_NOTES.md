# Нотатка для наступної сесії

Не історія (для цього `git log -- Docs/SESSION_NOTES.md`; повна версія до скорочення —
коміт `5aa40e8`) і не деталі (для цього `ROADMAP.md` §6 + `ls Docs/Lessons/`) — тільки те,
що з них не видно: на чому саме зупинились. Тримати ≤ ~50 рядків; довгі міркування — в ROADMAP.

## Де зупинились (2026-09-19)

**Урок 04 у процесі** (`Docs/Lessons/04_player_controller.md`). Рух — `CharacterController`.
Стан коду, який видно з диска:

- `Level_Arena.unity`: є `Plane` з `MeshCollider` (підлога, крок 2а) — **ще не Static**
  (`m_StaticEditorFlags: 0`); треба позначити для запікання NavMesh в уроці 08. Зміна не закомічена.
- `Assets/Resources/Player.prefab`: примітив Capsule, на ньому лише `CapsuleCollider`;
  `CharacterController` і `PlayerController` **ще нема**. Примітка: при додаванні
  `CharacterController` стандартний `CapsuleCollider` треба прибрати (CC сам є колайдером).
- `Assets/CodeBase/Player/` порожня. `GameLoopState` ще зі смоук-тестами (TestObject, лог Jump).

Далі: `CharacterController` на prefab → `PlayerController` з `Construct(IInputService)` → спавн у
`GameLoopState` (над підлогою, смоук-тести прибрати) → Play Mode → показати код на рев'ю.
Питання "Після уроку…" з файлу уроку — перевірити на рев'ю; ще спитати, як влаштований стрибок
(ручна вертикальна швидкість + гравітація).

## Висить і чекає на студента

- **Підтвердження Play Mode для `ServicesResolver`** (опційне ДЗ уроку 03). Без нього не оновлювати
  `PATTERNS.md`/`HISTORY.md`/`CV_LOG.md`/`ARCHITECTURE.md` під цю роботу.
- Студент попросив показати **варіант 3** резолвера ("жадібний" підбір конструктора з кількох)
  пізніше, окремо, лише для ознайомлення. Не пропонувати самому, дати, коли попросить.
- ⭐ до уроку 03 (другий `IInputService`) лише запропоновано, не підтверджено.

## Рішення, що діють

- Обсяг курсу розширено (2026-09-19) до 22 уроків + stretch; **карту не розширювати до закриття
  уроків 04–06** (нові ідеї — одним рядком у "пізніше", не в урок).
- Порядок: 04 → ⭐ Rigidbody-ДЗ (після рев'ю 04) → 05 камера → `05b` анімації → 06… Мілстоун-білди
  Standalone після 08/13/17; `12b` — перепис інфраструктури з пам'яті.
- Правило менторства: архітектурні рішення з best practice приймаю я й пояснюю чому; студент
  не отримує "меню" (ROADMAP §5, SKILL.md). Прийоми викладання — ROADMAP §5 "Стиль уроку".
- Тести: студент проходить свій курс з unit-тестів сам по відео; у уроки не вбудовуємо.
- `Docs/Notes/*.md` — лише на явне прохання студента.
- Git: пропоную коміти (назва+опис англійською), студент виконує сам.

## Довідкові шляхи (лише читання)

- Портативне дзеркало курсу Syndicate: `I:\UnityProjects\ref_for_claude` (лише C#-код).
  Повне дерево (prefab, сцени, Animator-контролери): `E:\syndicate\Architecture\k-syndicate.school\NN`,
  для анімацій — `13\...\Assets\Resources\{Hero,Enemies}`. Код IAP немає ніде, лише відео.
- Курс патернів GoF/SOLID (C#, Нестерук): `F:\Development\C#\Video Courses\Проектування\Code`.
- Mario/5 games (лише субтитри): `F:\Development\C#\5GamesUnityCourses\Code`.
- Unit-тести (відео без субтитрів, стартовий репо без тестів): `E:\syndicate\K-syndicate (Knowledge
  Syndicate) - Advanced Unit Testing in Unity`.
- **Математика/вектори/геометрія/кватерніони:** `I:\UnityProjects\Basic-Math-for-Game-Development`
  — власний репозиторій студента до книги *Basic Math for Game Development with Unity 3D*
  (Kelvin Sung, Gregory Smith, Apress 2023): розв'язки вправ `EX_N_M_MyScript.cs` по розділах
  2–9. Використовувати як джерело, коли урок торкається математики (карта розділ → урок у
  `SKILL.md`). Кінематики/гравітації в книзі немає — там `Docs/Notes/Kinematics_For_Jump.md`.
- Не показувати студенту код референсу напряму — лише форму (правило скіла).

## Спостереження про сам харнес

Пам'ять проєкту (`~\.claude\projects\...\memory`) містить лише 2 записи (11 старих на диску не
знайдено — причина невідома). `SKILL.md` ≈ 400 рядків — скоротити після закриття уроку 04, щоб не
чіпати специфікацію посеред уроку.
